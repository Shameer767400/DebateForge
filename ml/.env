# ── ML Service Environment ──
# Only include variables that the ML microservice actually needs.
# All other secrets (JWT, DB, etc.) belong in backend/.env only.

# Gemini / OpenAI — used by Whisper transcription fallback
GEMINI_API_KEY=AIzaSyDEFhSJPuocL72g2W4_jk0eRfBkpw8X6-k

# CORS — frontend origin for direct ML calls (if any)
FRONTEND_URL=http://localhost:3000

# Local Model Toggles
USE_LOCAL_STT=false
USE_LOCAL_TTS=false
USE_LOCAL_MEMORY=true
